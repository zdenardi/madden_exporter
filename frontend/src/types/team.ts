/**
 * TeamInfoResponse models the basic information about a team.
 * Equivalent to Python's TeamInfoResponse.
 */
export interface TeamInfoResponse {
    teamId: number;
    cityName: string;
    abbrName: string;
    nickName: string;
}